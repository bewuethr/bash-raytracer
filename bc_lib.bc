/* Dot product of two vectors */
define dot_prod(v1[], v2[]) {
    auto i, prod
    for (i = 0; i < 3; ++i)
        prod += v1[i] * v2[i]
    return prod
}

/* Absolute value */
define abs(val) {
    if (val > 0) {
        return val
    } else {
        return -val
    }
}
